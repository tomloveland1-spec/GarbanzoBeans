# Deferred Work

## Deferred from: code review of 1-1-project-scaffold-running-tauri-react-app (2026-04-04)

- **CSP is null** — `tauri.conf.json` has `"csp": null`. Needs a proper restrictive CSP before any public release. Complex to configure correctly for Tauri v2 + Vite dev server. Defer to Story 1.9 (Release Pipeline).
- **Windows-only CI matrix** — CI only runs on `windows-latest`. macOS/Linux builds not tested. Per architecture spec, macOS support is post-MVP. Add matrix when cross-platform work begins.
- **No lint in CI** — No `eslint` (frontend) or `cargo clippy` (Rust) step in the push/PR workflow. Add before first feature story lands.
- **`noUncheckedIndexedAccess` missing from tsconfig** — `strict: true` doesn't enable this. For a financial data app, array index access returning `T` instead of `T | undefined` is a silent error source. Add to `tsconfig.json` before Story 2.1 (first data model).
- **Placeholder icon files** — `src-tauri/icons/` contains 1×1 placeholder PNGs and a minimal 16×16 monochrome ICO. Need real branded icons before any public release or installer build. Defer to Story 1.9 (Release Pipeline).

## Deferred from: code review of 1-2-design-system-foundation-dark-forest-token-set (2026-04-06)

- **Google Fonts network dependency in Tauri app** — Roboto loaded from `fonts.googleapis.com` at runtime; fails silently offline. Per-spec (Task 1 specifies the Google Fonts link), but a local-first finance app should bundle the font. Consider bundling Roboto in a future story before public release.
- **Nav sidebar buttons have no active/selected state** — All four nav items render identically with no active indicator. Intentional placeholder; Story 1.4 replaces these with TanStack Router `<Link>` components that will handle active state.
- **Progress indeterminate state undetectable** — `value || 0` collapses `null`/`undefined` into same visual as `value={0}`. No indeterminate requirement exists in this story; revisit if a loading indicator is ever needed.
- **Google Fonts `<link>` missing `rel="preload"`** — The font stylesheet link is render-blocking with no preload hint. Minor performance issue; no spec requirement. Revisit if FOUT (flash of unstyled text) is observed.

## Deferred from: code review of 1-3-sqlite-infrastructure-database-migrations-and-atomic-writes (2026-04-06)

- **`updated_at` column never refreshed on row update** — `settings.updated_at` defaults to insert time and is never updated. No write commands exist in this story. Add an `AFTER UPDATE` trigger or application-layer update in Story 1.4 when write commands are introduced.
- **Migration MAX(version) logic skips back-filled versions** — The runner uses `MAX(version)` as the high-water mark. If a hotfix migration with a lower version number is added after a higher one is already applied, it is silently skipped forever. Only 1 migration currently; becomes a real risk when migrations 2+ are added. Consider switching to per-version membership checks before adding a second migration.
- **Poisoned Mutex has no recovery path** — If a thread panics while holding `DbState`'s Mutex, all subsequent DB commands return `DB_LOCK_POISON` permanently. Low probability in a single-user desktop app. Revisit if background threads are introduced.
- **`get_db_status` returns opaque DB_ERROR if `schema_version` absent** — If the command is somehow invoked before setup completes, the error code is indistinguishable from any other DB error. Theoretical given Tauri's setup guarantee; add a distinct `DB_NOT_INITIALIZED` code if background pre-fetch commands are added.
- **Single Mutex serializes all DB operations** — WAL mode is enabled for concurrent-read safety but the single `Mutex<Connection>` in `DbState` serializes every command. Connection pool (e.g., `r2d2` + `r2d2_sqlite`) is the correct solution. Deferred as a design decision per spec; revisit before any story that introduces parallel DB access.
- **Raw rusqlite error messages forwarded to frontend** — `From<rusqlite::Error>` uses `e.to_string()` which can expose table names, column names, and SQL fragments. Acceptable for a single-user desktop app with no telemetry. Revisit before adding any crash reporting or remote logging.

## Deferred from: code review of 1-4-app-state-foundation-zustand-stores-tanstack-router (2026-04-06)

- **Root loader silently continues on `loadSettings` failure** — `loadSettings` catches all errors internally; root loader resolves successfully even if DB is unavailable. App proceeds with `settings: null` and no user feedback. Consistent with arch "errors in error field" pattern; error display UI deferred to feature stories.
- **AC5 `isWriting` UI suppression scaffold only** — `isWriting` flag is correctly scaffolded but no feature component yet consumes it to suppress UI updates. Full suppression pattern implemented when feature components arrive in Epics 2–6.
- **`upsert_settings` COALESCE cannot clear nullable fields** — `COALESCE(?1, existing_col)` means passing `null` for a nullable field preserves the existing value rather than clearing it. Moot if `upsert_settings` is removed per D2 decision; if kept, Story 1.5 must address NULL-clearing behavior.
- **`onboarding_complete` accepts non-canonical `Option<i64>` values** — `UpsertSettingsInput.onboarding_complete: Option<i64>` accepts any integer; no clamping to 0/1. Moot if `upsert_settings` removed; if kept, add validation in Story 1.5.
- **`savings_target_pct` INSERT default undefined when passed as None** — Fresh INSERT with `input.savings_target_pct = None` sends SQL NULL; behavior depends on schema constraint. Moot if `upsert_settings` removed; if kept, add explicit default or enforce NOT NULL in Story 1.5.

## Deferred from: code review of 1-5-onboarding-first-launch-setup (2026-04-06)

- **`loadSettings` sets `isWriting: true` for read operations** — Misleading flag name causes spurious "Saving…" spinner during reads. Pre-existing from Story 1.3; rename to `isLoading` or use separate flags in a future refactor story.
- **`upsert_settings` COALESCE prevents field nullification once set** — Acceptable for onboarding where all fields are always provided; spec documents this limitation. Story 1.6 (Settings Screen) must handle explicit clearing differently if needed.
- **`pastTwelveMonths` uses local time** — One-month shift at midnight/month-boundary for users in non-UTC zones. Low real-world impact; revisit if user-reported date confusion emerges.
- **E2E double `addInitScript` ordering** — `injectTauriMock` in `beforeEach` plus in-test `addInitScript` override on `__TAURI_INTERNALS__`. Playwright guarantees registration order so not currently broken; refactor into a single mock factory when E2E suite grows.
- **`budget_name` non-breaking space (U+00A0) passes `.trim()`** — Stored as invisible whitespace. Niche input edge case; add Unicode-aware trim in a future input hardening pass.
- **`guardTurnTheMonth` does not handle `'closed'` monthStatus** — Pre-existing from Story 1.4; no current story produces `'closed'` state. Address in Epic 6 (Turn the Month).
- **`unchecked_transaction` in migrations — theoretical interleave** — `unchecked_transaction()` does not verify no transaction is active. Not a bug given current call pattern (always autocommit); add a defensive assertion if background DB threads are introduced.
- **`guardOnboarding` exported from router module** — Exported to enable unit-test import; exposes an internal routing guard on the module's public surface. Refactor to a co-located `router.test.ts` if the module API surface needs to be tightened.

## Deferred from: Story 1.7 sentinel-lock-read-only-mode (2026-04-06)

- **`settings.spec.ts` navigate-away E2E test fails with "expected 30, received 10"** — `page.goto()` causes a full page reload; `addInitScript` re-runs with the original serialized settings, resetting the mock's in-memory `storedSettings`. The save mutation is lost on reload. Pre-existing failure (was already timing out before Story 1.7). Fix: use Playwright's `page.evaluate()` to mutate the live `__TAURI_INTERNALS__` mock after save, or add `get_read_only_state` to the mock and avoid reloads. [`e2e/settings.spec.ts:98`]
- **`e2e-integration/sentinel-lock.test.ts` is a stub** — Full WebdriverIO + tauri-driver setup required to run real two-instance sentinel detection against the built binary. Setup instructions documented in the stub file. Defer to Story 1.9 (Release Pipeline) when CI build infrastructure is in place. [`e2e-integration/sentinel-lock.test.ts`]

## Deferred from: code review of 1-7-sentinel-lock-read-only-mode (2026-04-06)

- **`isReadOnly` not refreshed if the other instance closes during the session** — `is_read_only` is frozen at startup; if the second instance closes mid-session, the first stays read-only until restarted. Spec acknowledges detection is init-only; add a session-refresh mechanism if this proves frustrating in practice. [`src-tauri/src/lib.rs:58-63`]
- **`.ok()` silently discards non-`QueryReturnedNoRows` rusqlite errors in sentinel query** — The `.optional().ok().flatten().flatten()` chain converts any rusqlite error (schema mismatch, I/O) to `None`, skipping the sentinel check and defaulting to read-write on a broken database. Low practical risk given current single-connection setup. [`src-tauri/src/lib.rs:47-56`]
- **E2E integration stub uses `browser.closeWindow()` which may not trigger Tauri `CloseRequested`** — The "deletes lock file after normal close" test calls `browser.closeWindow()` (WebdriverIO window-level); Tauri's `CloseRequested` event fires on OS-level close. The close handler may not run, making the test ineffective. Revisit when activating stub in Story 1.9. [`e2e-integration/sentinel-lock.test.ts:84`]

## Deferred from: code review of 1-8-auto-update-with-user-confirmation (2026-04-06)

- **WAL checkpoint silently skipped under lock contention** — `try_lock()` on `DbState` in `CloseRequested` handler silently skips `wal_checkpoint` if any command is in-flight. Data is not lost (WAL replay on next launch), but database stays non-checkpointed permanently under sustained contention. Pre-existing from Story 1-7 sentinel design. [`src-tauri/src/lib.rs:91`]
- **DataFolderState stale after `upsert_settings` changes data folder path** — `DataFolderState` is populated once at startup and never updated. If the user changes their data folder via Settings, the close handler calls `sentinel::release` on the old path, leaving the new path's lock file intact and causing next-launch read-only mode. Pre-existing from Story 1-7 sentinel design. [`src-tauri/src/lib.rs:69`]
- **Sentinel latent trap — DataFolderState stored unconditionally for read-only instances** — Path is managed unconditionally; correctness of not releasing the sentinel depends entirely on the `!is_read_only` guard remaining intact. A future refactor weakening that guard would silently release a lock owned by another instance. Pre-existing from Story 1-7 sentinel design. [`src-tauri/src/lib.rs:105`]

## Deferred from: code review of 2-1-envelope-schema-data-model (2026-04-06)

- **Implicit transaction rollback without explicit rollback** — rusqlite correctly rolls back on drop, but the absence of an explicit `tx.rollback()` before early-return errors is a copy-paste trap. If future inner functions perform multiple writes before a `changes()` check, partial writes will silently roll back. [`src-tauri/src/commands/mod.rs`]
- **`isWriting` used for `loadEnvelopes` read operation** — `loadEnvelopes` sets `isWriting: true` for a SELECT, not a mutation. Any UI disabling write controls on `isWriting` will incorrectly block creates/updates during a read. Pre-existing pattern; same as `loadSettings`. Consider renaming to `isBusy` or adding a separate `isLoading` in a future refactor. [`src/stores/useEnvelopeStore.ts`]
- **`loadEnvelopes` fires on every navigation in `beforeLoad`** — `rootRoute.beforeLoad` runs on every navigation, re-fetching all envelopes on every route change. Consistent with `loadSettings` pattern per spec; revisit if list grows or a staleness check is needed. [`src/router.tsx`]
- **Rollback snapshot stale on concurrent writes** — `prev` snapshot captured before optimistic update; if a concurrent write completes between snapshot and error, the rollback overwrites the intervening result. Mitigated by `isWriting` UI guard per spec; revisit if any code path bypasses the guard. [`src/stores/useEnvelopeStore.ts`]
- **Whitespace name renders blank during optimistic window** — A whitespace-only name passes into the optimistic update and displays as blank before Rust rejects it and the store rolls back. Correct end state; brief visual artifact only. [`src/stores/useEnvelopeStore.ts`]
- **`unchecked_transaction()` bypasses borrow-check transaction safety** — Required by `&Connection` signatures used for testability; safe under current single-connection setup. Revisit if helpers are refactored to accept `&mut Connection`. [`src-tauri/src/commands/mod.rs`]
- **`update_envelope` COALESCE cannot clear `month_id` to NULL** — `month_id = COALESCE(?6, month_id)` treats `None` as "no change"; there is no way to explicitly null-out `month_id`. TypeScript optimistic update applies `null` locally, so store and DB diverge on a clear operation. Zero practical impact until month assignment UI is built (Epic 2.4+). **Requires an explicit fix when month assignment feature is implemented — Epic 6 FK wiring alone will not resolve this.** [`src-tauri/src/commands/mod.rs`, `src/stores/useEnvelopeStore.ts`]

## Deferred from: QA Walkthrough — Design Concern (2026-04-07)

- **Settings Save exposes raw SQL error to user** — Clicking Save with no changes shows `NOT NULL constraint failed: settings.onboarding_complete` in red below the button. Raw SQLite constraint errors must never reach the UI. The Settings page `upsert_settings` call is not including `onboarding_complete` in its payload, violating the NOT NULL constraint. Fix: include `onboarding_complete` in the Settings save payload (preserve existing value), and replace any raw error display with a user-friendly message. [`src/features/settings/SettingsPage.tsx`, `src-tauri/src/commands/mod.rs`]
- **Settings Save button always enabled** — Save button is bright lime regardless of whether any changes have been made. Spec requires it to be disabled until a field is modified. As a side effect, the always-enabled state allows the SQL error above to be triggered. [`src/features/settings/SettingsPage.tsx`]
- **Settings screen missing Budget Name and Budget Start Month fields** — Both values are set during onboarding but cannot be changed in Settings. Spec requires Budget Name field here. Budget Start Month should also be editable post-onboarding. [`src/features/settings/SettingsPage.tsx`]
- **Terminology: "Envelope" may be too jargon-heavy** — Tom prefers something like "Category Budget" as a more self-explanatory term for new users. "Envelope" is YNAB-native and familiar to power users but opaque to newcomers. Decision needed before any marketing or onboarding copy is finalized. Consider whether to rename globally or use "envelope" internally and friendlier language in the UI.
- **Read-only mode does not suppress write actions** — When a second instance opens in read-only mode, the Add Envelope button remains active and functional. Confirmed: user was able to create envelopes in the read-only instance. Create, Edit, and Delete must all be blocked when `isReadOnly = true`. The read-only banner appears correctly but the UI enforcement is missing. [`src/features/envelopes/`, `src/stores/useEnvelopeStore.ts`]
- **⋯ button has no action menu — Edit flow is incomplete** — Clicking ⋯ goes directly to delete confirmation; no intermediate action menu with Edit/Delete. Envelope name is editable inline (clicking the name field), but type (Rolling/Bill/Goal) and category (Need/Want) have no edit path whatsoever. Full edit form needs to be built and wired to the ⋯ menu. [`src/features/envelopes/EnvelopeCard.tsx`]
- **Envelope card shows single unlabeled amount** — Card displays one `$0.00` with no label indicating whether it represents allocated, spent, or remaining. Spec requires all three values shown with clear labels. Currently ambiguous to the user. [`src/features/envelopes/EnvelopeCard.tsx`]
- **Budget screen empty state copy missing** — When no envelopes exist, the screen shows only the "Add Envelope" button with a blank canvas. Spec requires an empty state message (e.g., "No envelopes yet. Add one to start budgeting.") to guide new users. [`src/features/envelopes/` or budget screen component]
- **Onboarding resets to splash screen mid-flow** — During onboarding, the app intermittently drops back to the "Get Started" splash screen, losing all entered values and step progress. Trigger is unclear — could be focus loss, Vite HMR firing on a timer, or the router re-evaluating the guard. Fix: persist step state incrementally (DB or sessionStorage) so a re-mount restores position regardless of cause. [`src/features/onboarding/`]
- **Onboarding should be a visual teaching experience, not just a data collection form** — Each step should pair a compelling graphic with copy that explains *why* the feature matters before asking for input. Example for step 4 (savings target): show a wealth/growth graphic and say something like "This is where wealth is built — your savings target keeps you protected and growing month after month." Currently all steps are plain forms with no visuals. This is a UX uplift for the full 4-step flow. Prioritize before any public release or user testing.
- **Onboarding step 4 copy frames savings target as a hard limit** — "What percentage of income do you want to save?" implies a commitment or ceiling. This field is an aspirational stretch goal — the heart of the wealth-building feature. Suggested reframe: something like "Set a savings goal" with subtext like "This is your target — we'll track your progress toward it each month." Revisit copy before Epic 5 (Savings & Wealth Tracking) work begins.
- **Pay schedule removed from onboarding — named paychecks deferred to Epic 2.4** — Pay frequency/date collection removed from onboarding (2026-04-07). Tom's direction: instead of fixed dates, users should name their income streams (e.g. "1st Paycheck", "2nd Paycheck") and allocate each paycheck to envelopes at fill time. This is a design-first feature for Story 2-4 (envelope allocation / monthly planning session). The Settings screen still has pay schedule fields — those should also be evaluated for removal/replacement when 2-4 is designed.

## Deferred from: QA Walkthrough — Section 2, Onboarding (2026-04-07)

- **Onboarding layout not vertically centered** — Form content (heading, fields, buttons) is crammed into the lower-left of the screen with a large empty area above. Content should be centered or at minimum padded to a visually balanced position. [`src/features/onboarding/` or equivalent]
- **Budget start month displays raw ISO format** — Dropdown shows `2026-04`, `2026-03`, etc. instead of `April 2026`, `March 2026`. Unfriendly for users. Format display values using `Intl.DateTimeFormat` or equivalent. [`src/features/onboarding/`]
- **Budget start month should be a month picker, not a list** — A scrollable list of ISO strings is not the right control for month selection. Should be a proper month/year picker (e.g., shadcn calendar in month mode, or a two-field month+year select). [`src/features/onboarding/`]
- **Month dropdown overlaps form fields** — Dropdown opens upward and covers the "Name your budget" heading and Budget Name input due to the layout positioning. Resolved by fixing the layout bug above, but also verify dropdown z-index and opening direction are correct once layout is fixed. [`src/features/onboarding/`]

## Deferred from: QA Walkthrough — Section 1, Global UI & Design System (2026-04-07)

- **Sidebar responsive collapse not implemented** — Spec requires sidebar to collapse to 48px icon-only mode at <1024px window width. No icons exist; sidebar remains full-width text-only regardless of window size. Post-MVP responsive feature — address before any mobile/compact breakpoint work begins.
- **TanStack Router devtools badge visible in dev build** — A "TanStack Router" dev badge appears at the bottom-left corner of the app window. Verify this is suppressed in production/release builds (should only appear in `NODE_ENV=development`). If not already guarded, add a `process.env.NODE_ENV !== 'production'` guard around the devtools component.
- **Sidebar nav hover state not implemented** — Hovering inactive nav items (Ledger, Rules, Settings) produces no visual change. Spec requires `rgba(255,255,255,0.07)` background tint on hover, matching the active state tint. [`src/features/navigation/` or equivalent sidebar component]
- **White flash on launch (FOUC)** — App window opens white before the dark theme loads, then snaps to `#111214`. Spec explicitly requires no flash of unstyled content. Fix: set `background-color: #111214` on `<html>` or `<body>` in `index.html` so the window is dark before React hydrates.

## Deferred from: code review of 2-2-create-and-manage-envelopes (2026-04-06)

- **FK constraint errors on `DELETE` surface as generic rusqlite errors** — Once Epic 6 enables `PRAGMA foreign_keys = ON`, deleting an envelope with child rows will raise a constraint error with no domain-specific code (no `CONSTRAINT_VIOLATION` or similar). `delete_envelope_inner` has no match on error type. [`src-tauri/src/commands/mod.rs`]
- **All-`None` `UpdateEnvelopeInput` performs a no-op UPDATE that returns success** — Calling `update_envelope_inner` with only `id` set (all other fields `None`) runs a COALESCE no-op, returns `changes()=1`, and returns the unchanged envelope as if it was updated. Semantic oddity; no visible bug today. [`src-tauri/src/commands/mod.rs`]
- **`aria-label="Envelope settings"` on ⋯ button is misleading** — Button only opens a delete confirmation dialog, not a settings panel. Screen reader users will be confused. Minor UX debt. [`src/features/envelopes/EnvelopeCard.tsx`]
- **`formatCurrency` silently renders `NaN`/`Infinity` for corrupt `allocatedCents`** — `Intl.NumberFormat.format(NaN/100)` renders `"NaN"` in the UI. No guard or test for this case. [`src/lib/currency.ts`]

## Deferred from: code review of 2-4-envelope-allocation-monthly-planning-session (2026-04-07)

- **`loadIncomeEntries` sets `isWriting: true` for a read operation** — same pre-existing pattern as `loadEnvelopes` and `loadSettings`; conflates read/write state. Rename to `isBusy` or add a separate `isLoading` flag in a future refactor. [`src/stores/useIncomeStore.ts:24`]
- **`allocate_envelopes_inner` re-SELECT SQL uses dynamic positional params** — `WHERE id IN (?1, ?2, ...)` hits SQLite's `SQLITE_MAX_VARIABLE_NUMBER` limit (default 999) at 1000+ envelopes. Pathological for MVP; refactor to chunked batches or a temp table if the envelope count ever approaches that ceiling. [`src-tauri/src/commands/mod.rs`]
- **Concurrent income entry mutations can interleave optimistic state** — no `isWriting` gate on `createIncomeEntry`/`deleteIncomeEntry`; rapid concurrent calls risk a rollback overwriting a committed result. Theoretical in single-user desktop context; revisit if any background process ever triggers mutations. [`src/stores/useIncomeStore.ts`]
- **`loadIncomeEntries` in root `beforeLoad` has no error handling** — same architectural pattern as `loadEnvelopes`; an unhandled throw propagates to TanStack Router and can break navigation. Consistent with existing arch; add a top-level error boundary before any crash reporting is introduced. [`src/router.tsx:59`]
- **Optimistic delete can target a temp (negative) ID during in-flight create** — if the user clicks delete on an optimistically-added entry before the `create_income_entry` invoke resolves, the backend returns `ENTRY_NOT_FOUND`, the rollback restores the pre-delete list, but the in-flight create may complete and leave an orphaned entry depending on timing. Very hard to trigger in practice. [`src/stores/useIncomeStore.ts`]

## Deferred from: code review of 2-3-envelope-state-logic-traffic-lights-tooltips (2026-04-07)

- **`borderLeft` + `border` shorthand on same element — fragile under refactoring** — `EnvelopeCard.tsx` sets `border: '1px solid var(--color-border)'` and `borderLeft: '4px solid ...'` in the same style object. Works correctly today (React processes keys in order); if ever merged with a spread or reordered, `border` shorthand resets the left edge and the traffic-light color silently disappears. [`src/features/envelopes/EnvelopeCard.tsx`]
- **`deriveEnvelopeState` treats negative `allocatedCents` as `funded`** — Logic checks `allocatedCents === 0` for caution and `spentCents > allocatedCents` for overspent; a negative `allocatedCents` (e.g. from a DB bug) passes both guards and returns `funded` with a green badge. Add non-negative input guard or clamp when Epic 3 wires real spending data. [`src/lib/envelopeState.ts`]
- **Tooltip test may be brittle in CI** — `userEvent.hover` + `findByRole('tooltip')` relies on Radix portal behavior in jsdom. Works locally with `delayDuration={0}`; revisit if test flakiness is observed in CI. [`src/features/envelopes/EnvelopeCard.test.tsx`]
- **`<Badge>` as `TooltipTrigger` is non-interactive — keyboard users cannot access tooltip** — `Badge` renders a `div`; `TooltipTrigger asChild` merges props onto it but keyboard focus is not possible. Screen readers and keyboard-only users cannot trigger the state explanation tooltip. Add a `tabIndex` or replace with a `<button>` wrapper when accessibility is prioritized. [`src/features/envelopes/EnvelopeCard.tsx`]

## Deferred from: code review of 1-6-settings-screen-update-configuration-anytime (2026-04-06)

- **`useState` stale on async settings load** — All form state initialized from `settings` at first render; if store updates while component is mounted, form won't reflect changes. Mitigated by `guardOnboarding()` ensuring `settings` is non-null before `/settings` is reachable; unmount/remount pattern is intentional per spec (AC4). [`src/features/settings/SettingsPage.tsx`]
- **`handleSave` concurrent invocation race** — `isWriting` store flag prevents double-submit in most cases, but flag update is not guaranteed before a second synchronous click event. Theoretical; pre-existing pattern used throughout the codebase. [`src/features/settings/SettingsPage.tsx`]
- **`buildPayDates` accepts empty strings with no internal guard** — A pure utility function with no defensive validation; relies entirely on caller's `isSaveDisabled` check. By design (pure transform); no call site bypasses the guard. [`src/lib/pay-dates.ts`]
- **E2E test doesn't assert `upsert_settings` called with correct payload** — The navigate-away-and-return test verifies user-visible behavior but not the persistence contract. A no-op `upsertSettings` that cached in memory would still pass. Minor coverage gap. [`e2e/settings.spec.ts`]
- **`parsePayDates` format mismatch produces silent empty fields** — If stored `payFrequency` / `payDatesJson` format diverges (e.g., from a manual DB edit or a future migration), `parsePayDates` returns empty strings with no user explanation. Defensive fallback behavior; no mismatched data is written by the current codebase. [`src/lib/pay-dates.ts`]

## Deferred from: code review of 2-5-borrow-flow-covering-a-shortfall (2026-04-07)

- **`borrow_events` has no FK constraints on envelope IDs** — `source_envelope_id` and `target_envelope_id` are `NOT NULL` but carry no `REFERENCES envelopes(id)` constraint. Deleting an envelope after a borrow leaves orphaned event rows with dangling IDs. Pre-existing pattern; address when borrow history is surfaced in closeout summary (FR19, Epic 6). [`src-tauri/migrations/005_borrow_schema.sql`]
- **Borrow button `overspent` condition is dead code** — `deriveEnvelopeState` receives `spentCents=0` always; the `'overspent'` state is unreachable until Epic 3 wires real spending data. Intentional forward-compatibility; revisit when EnvelopeCard is connected to transaction data. [`src/features/envelopes/EnvelopeCard.tsx:205`]
- **No index on `borrow_events` FK columns** — `source_envelope_id` and `target_envelope_id` have no index. Any future query for borrow history per envelope will full-scan the table. Trivially cheap to add; defer to whenever borrow history reporting is built (Epic 6). [`src-tauri/migrations/005_borrow_schema.sql`]

## Deferred from: Story 2-5-borrow-flow-covering-a-shortfall (2026-04-07)

- **Runway estimate omitted from savings borrow confirmation** — AC4 specifies the body of the savings confirmation dialog shows "resulting savings balance and runway estimate." For Story 2.5, only the new savings balance is shown (`New savings balance: ${formatCurrency(savingsBalance - amount)}`). Runway calculation requires Epic 5 (FR21–FR25) infrastructure. Add runway line to `BorrowOverlay` savings confirmation view when `useRunwayStore` (or equivalent) is available in Epic 5. [`src/features/envelopes/BorrowOverlay.tsx`]
- **No DB constraint enforcing "at most one savings envelope"** — `is_savings = 1` can be set on multiple envelopes simultaneously. The UI (checkbox in `AddEnvelopeForm`) does not unset `is_savings` on other envelopes when a new one is marked as savings. MVP acceptable for single-user; add a constraint or UI enforcement if multi-savings confusion is observed. [`src-tauri/migrations/005_borrow_schema.sql`, `src/features/envelopes/AddEnvelopeForm.tsx`]

## Deferred from: code review of 3-1-transaction-schema-data-model (2026-04-07)

- **W1: No explicit `tx.rollback()` on early return** — `allocate_envelopes_inner` and `delete_income_entry_inner` rely on rusqlite's drop-rollback guarantee rather than calling `tx.rollback()` explicitly. Correct in practice; add explicit rollback for defensive clarity if the pattern is revisited. [`src-tauri/src/commands/mod.rs`]
- **W2: Borrow source-balance TOCTOU under WAL** — The `SELECT` + guard inside the borrow transaction prevents concurrent overwrites only under SQLite's default journal mode. If WAL mode is ever enabled, two concurrent borrow operations could both pass the balance check. Not a concern for single-writer desktop use. [`src-tauri/src/commands/mod.rs`]
- **W3: Optimistic `createTransaction` appends at wrong sort position** — New optimistic entry goes to end of array; store is `ORDER BY date DESC`. The entry stays out-of-order until next `loadTransactions`. Address when `LedgerView` (Story 3.3) is implemented by sorting after replace or reloading after create. [`src/stores/useTransactionStore.ts`]
- **W4: `date` field not validated as ISO 8601** — `create_transaction_inner` stores the `date` string as-is with no format check. A malformed date (e.g. `"04/07/2026"`) breaks the date-range filter silently. Add a validation helper or DB CHECK constraint when input sanitization story is scoped. [`src-tauri/src/commands/mod.rs`]
- **W5: `beforeLoad` store chain not wrapped in try/catch** — An uncaught exception from `loadIncomeEntries` leaves `loadTransactions` unreached. Pre-existing pattern across all store loads. Address holistically when app error boundary work is scoped. [`src/router.tsx`]
- **W6: Concurrent `loadTransactions` during in-flight `createTransaction` orphans optimistic entry** — `loadTransactions` clobbers the full transactions array, dropping any in-flight temp entries. Not reachable with current navigation, but must be addressed before any route-driven reload can fire during a write. [`src/stores/useTransactionStore.ts`]
- **W7: `borrow_from_envelope` does not block savings envelopes as source** — `is_savings` flag is stored but never consulted during borrow operations. Savings balance integrity is a Story 5.x concern. [`src-tauri/src/commands/mod.rs`]
- **W8: `delete_transaction` command absent** — Intentionally deferred per spec's Deferred Items. Transactions created in error require direct DB manipulation until a delete command is implemented. Scope with Story 3.5 (manual transaction entry and editing). [`src-tauri/src/commands/mod.rs`]
- **W9: `guardReadOnly` not reactive after route load** — Route guards evaluate `isReadOnly` only at `beforeLoad` time. If sentinel state changes while user is on a guarded route, they are not redirected. Pre-existing pattern. Address with a global sentinel listener if live lock enforcement is required. [`src/router.tsx`]

## Deferred from: code review of 3-2-ofx-import-file-ingestion-and-atomic-commit (2026-04-07)

- **unchecked_transaction() footgun** — Matches existing codebase pattern and not currently buggy; correct fix is `conn.transaction()` for compile-time lifetime safety. Revisit if import command is ever refactored. [`src-tauri/src/commands/mod.rs`]
- **Drag-drop useEffect empty deps []** — Currently safe because handleImport calls `getState()` rather than closing over component state; `eslint-disable` comment suppresses lint. Flag if handleImport is ever refactored to close over props/state. [`src/features/transactions/OFXImporter.tsx`]
- **No file size limit before read_to_string** — OOM risk on maliciously large OFX files; not an NFR in this story. Add a metadata size check before `read_to_string` in a hardening pass. [`src-tauri/src/commands/mod.rs`]
- **importOFX appends without FITID deduplication** — Double-importing the same OFX file duplicates all transactions. Story 3.4 (auto-match clearing) is the appropriate home for deduplication logic. [`src/stores/useTransactionStore.ts`]
- **createTransaction optimistic rollback uses stale snapshot under concurrent calls** — Pre-existing store design; race only reachable if two creates are in-flight simultaneously. No current UI triggers this. [`src/stores/useTransactionStore.ts`]
- **ofxDateToIso: no calendar validation** — Month 13 or day 99 passes the digit check and is stored as-is. No current logic breaks on invalid ISO date strings. Add a range check if date-based queries become stricter. [`src/lib/parseOFX.ts`, `src-tauri/src/commands/mod.rs`]
- **generate_batch_id unwrap_or(0) on pre-epoch clock** — System clock before 1970 produces `import_0` for all batches. Extremely unlikely on any real hardware. [`src-tauri/src/commands/mod.rs`]
- **map_transaction_row hardcoded column indices** — Consistent with the rest of the codebase. Silent breakage risk if SELECT column order changes. [`src-tauri/src/commands/mod.rs`]
- **LedgerPage no Suspense/error boundary** — getCurrentWebview() throws outside Tauri; no current CI test hits this path. Add an error boundary if web-based test rendering is introduced. [`src/features/transactions/LedgerPage.tsx`]
- **Whitespace-only payee stored as empty string** — When NAME and MEMO are both absent/whitespace, payee is "". Visible in the ledger; not a crash. Consider a "(unknown payee)" placeholder in a UX polish pass. [`src/lib/parseOFX.ts`]
- **Payee length unbounded** — No truncation or max-length check. SQLite TEXT has no practical limit. Not a spec requirement. [`src/lib/parseOFX.ts`, `src-tauri/src/commands/mod.rs`]
- **Amount overflow for values near i64::MAX** — `f64 * 100.0 as i64` truncates silently for amounts like $92 trillion. Not realistic for bank data. [`src-tauri/src/commands/mod.rs`]
- **No transaction date range validation** — Dates like year 2099 or 1900 pass through. No current ledger logic breaks on out-of-range dates. [`src/lib/parseOFX.ts`]
- **Drag-drop event can fire twice on some OS combinations** — No dedup guard on the drop handler; rapid double-drop triggers two concurrent imports. Not in scope; guard with a processing flag if reported. [`src/features/transactions/OFXImporter.tsx`]
- **NaN/invalid amount silently skips transaction with no user feedback** — The complete-state count of 0 (or fewer than expected) is the only signal. Consider tracking and surfacing a skipped-count in a future import UX pass. [`src/lib/parseOFX.ts`]
- **Parallel TS/Rust parsers have no cross-validation** — `parseOFX.ts` and `parse_ofx_sgml` implement the same OFX parsing algorithm independently with no shared test suite; a future divergence between the two would be silent and could cause preview-vs-stored value mismatches. [`src/lib/parseOFX.ts`, `src-tauri/src/commands/mod.rs`]

## Deferred from: code review of 3-3-dual-balance-ledger-view (2026-04-08)

- **No error/loading state surfaces `loadTransactions` failure** — `LedgerPage` and `LedgerView` don't read `error` from the store; user sees "No transactions yet" empty-state when the DB load fails with no indication of the real problem. Systemic pattern; address holistically when an app-level error boundary is scoped. [`LedgerPage.tsx:9-11`]
- **Race condition: in-flight `loadTransactions` can overwrite freshly imported transactions** — `loadTransactions` does `set({ transactions })` (full replace); if it resolves after `importOFX` has already appended its results, the imported rows are silently dropped until next remount. Pre-existing store design from Story 3.2. [`src/stores/useTransactionStore.ts`]
- **`envelopeMap` and balance reductions not memoized** — `new Map(...)`, `filter().reduce()`, and `reduce()` run unconditionally on every render. Minor perf with a small dataset; becomes noticeable with hundreds of transactions. Add `useMemo` when performance warrants it. [`LedgerView.tsx:26-33`]
- **Sort order has zero test coverage** — `LedgerView` renders transactions in store order with no `.sort()` call, correctly relying on the SQL `ORDER BY date DESC, id DESC` contract. If that contract is ever broken, no frontend test will catch it. Consider a sort-order test or explicit contract comment. [`LedgerView.test.tsx`]
- **Import summary shows "0 transactions" for fully-deduplicated imports** — When `importResult.count = 0` and `latestDate` is non-null, the banner renders "Import — [date] — 0 transactions". Technically correct but potentially confusing when all rows were deduplicated. Low priority; address in a UX polish pass. [`LedgerView.tsx`]

## Deferred from: code review of 3-4-auto-match-clearing-imported-transactions (2026-04-08)

- **`unchecked_transaction` borrow safety pattern** — pre-existing codebase pattern used throughout `commands/mod.rs`; correct fix is `conn.transaction()` for compile-time lifetime safety. Revisit if import logic is refactored. [`src-tauri/src/commands/mod.rs`]
- **Rust auto-match tests use predictable fixed filenames in `std::env::temp_dir()`** — `test_automatch1.ofx` through `test_automatch5.ofx` use fixed names; parallel `cargo test` runs could collide with leftover files from a prior run. Low risk since tests write before reading. Use `tempfile` crate or append a timestamp/UUID if test flakiness is observed. [`src-tauri/src/commands/mod.rs` — `mod import_tests`]
- **Re-importing the same OFX file inserts duplicate cleared rows** — After a successful import all matched/inserted rows have `is_cleared=1`; a second import finds no uncleared candidates and re-inserts everything as new rows. No FITID-based deduplication is implemented. Pre-existing limitation noted in story 3.2. Scope FITID deduplication in a future OFX hardening story. [`src-tauri/src/commands/mod.rs` — `import_ofx_inner`]
- **`ofx_amount_to_cents` silently truncates sub-cent precision** — Input like `-10.005` truncates fractional digits beyond two decimal places, yielding 1000¢ instead of 1001¢. US bank data rarely emits this but a mismatch would cause the auto-match to fail and insert a duplicate. Pre-existing from story 3.2. [`src-tauri/src/commands/mod.rs`]
- **Unicode minus sign (U+2212) in OFX amount fields causes silent transaction drop** — `s.starts_with('-')` checks ASCII only; a U+2212 minus causes `parse::<i64>()` to return `Err` and the transaction is skipped with no user feedback. Pre-existing from story 3.2. [`src-tauri/src/commands/mod.rs`]
- **Imported transactions appended to store regardless of active month filter** — After `importOFX`, `result.transactions` are appended to `state.transactions` without checking whether they belong to the currently loaded month. Out-of-month rows appear in the view until next `loadTransactions`. Address when month-scoped import UX is designed. [`src/stores/useTransactionStore.ts`]
- **`importOFX` in-place update is a no-op if `loadTransactions` was never called** — If the user imports before the ledger has loaded, `state.transactions` is empty; `result.matchedTransactions` are updated in-place over nothing and are invisible in UI until a reload. Consider calling `loadTransactions` after import, or loading on import screen mount. [`src/stores/useTransactionStore.ts`]
- **`generate_batch_id` atomic counter resets to 0 on process restart** — If the app crashes and restarts within the same millisecond, `batch_id` could collide with a prior session's batch. `batch_id` is not a unique key in the DB so this causes grouping confusion only, not data corruption. Extremely unlikely. [`src-tauri/src/commands/mod.rs`]

## Deferred from: code review of 3-5-manual-transaction-entry-and-editing (2026-04-08)

- **loadEnvelopes called on every category commit including no-op re-selections** — After any category change in `TransactionRow.commitEdit`, `loadEnvelopes()` is always called even when the user selects the same envelope they already had, causing an unnecessary backend round-trip. [`src/features/transactions/TransactionRow.tsx`]
- **`today` default date computed once at AddTransactionForm mount** — If the form remains open across midnight, the default date field shows the previous day. Low probability but worth a future fix. [`src/features/transactions/AddTransactionForm.tsx`]
- **updateTransaction errors silently swallowed in TransactionRow inline edits** — When a Tauri `updateTransaction` call fails, the error is not surfaced to the user; the edit appears to complete but the data is not saved. [`src/features/transactions/TransactionRow.tsx`]
- **Date input in TransactionRow does not validate format before persisting** — Malformed date strings from data corruption would pass through `commitEdit('date')` and be sent to the backend without client-side validation. [`src/features/transactions/TransactionRow.tsx`]

## Deferred from: code review of 4-1-merchant-rules-schema-data-model (2026-04-08)

- **Integer overflow in ofx_amount_to_cents** — `n * 100` and `int_cents + frac_cents` use unchecked arithmetic; for extreme amounts (≥ i64::MAX / 100) this silently wraps. Use `checked_mul`/`checked_add`. Pre-existing OFX code, outside story 4.1 scope. [`src-tauri/src/commands/mod.rs`: `ofx_amount_to_cents`]
- **loadRules failure in rootRoute.beforeLoad has no recovery path** — If `loadRules()` rejects, the route load crashes with no retry. Consistent with the same pattern used by `loadEnvelopes`, `loadTransactions` etc.; address holistically when a global error boundary or retry mechanism is added. [`src/router.tsx`, `src/stores/useMerchantRuleStore.ts`]
- **conflictingRules() recomputes O(n²) on every call with no memoization** — The store method runs a full nested-loop containment check on every invocation. Acceptable now (no UI consumer yet, rule counts expected to be small), but should be memoized if called per-render once the rules screen ships in Story 4.6. [`src/stores/useMerchantRuleStore.ts`]

## Deferred from: code review of 4-2-auto-categorization-on-import (2026-04-08)

- **`importOFX` concurrent calls can produce duplicate rows in store** — If two `importOFX` calls are dispatched before either resolves, both append their `result.transactions` to the store. Single-user desktop app makes this unlikely; address if a debounce/guard is added to the import flow. [`src/stores/useTransactionStore.ts` — `importOFX`]
- **`_tempIdCounter` module-level counter not reset across store recreations** — Negative temp IDs don't collide with positive DB IDs in practice, but a global reset hook would be safer. Pre-existing design. [`src/stores/useTransactionStore.ts`]
- **`unchecked_transaction()` instead of `conn.transaction()`** — rusqlite requires `&mut Connection` for `transaction()`; `import_ofx_inner` takes `&Connection`, making `unchecked_transaction` the only option. Consider refactoring call sites to pass `&mut Connection` for stronger atomicity guarantees. [`src-tauri/src/commands/mod.rs` — `import_ofx_inner`]
- **`generate_batch_id` sequence resets on process restart** — The sequential counter within a process restart doesn't prevent theoretical ID collisions across sessions (millis + seq). Acceptable for single-user use; a UUID-based batch ID would eliminate the risk entirely.
- **`envelopeMap.get()` returns "Unknown" for deleted envelopes** — Pre-existing `?? 'Unknown'` fallback pattern shared with `TransactionRow`. No distinct UX for a deleted-envelope scenario. [`src/features/transactions/LedgerView.tsx`]

## Deferred from: code review of 4-3-unknown-merchant-queue-manual-categorization (2026-04-08)

- **`formatTxDate` appends `T00:00:00` without validating date format** — Pre-existing pattern copied from `TransactionRow`; spec explicitly calls out this pattern. If backend ever returns a datetime string instead of a date-only string, "Invalid Date" renders silently. [`src/features/transactions/UnknownMerchantQueue.tsx:14`]
- **`importOFX` result.transactions not deduplicated against result.matchedTransactions** — If the backend places an ID in both arrays, it appears twice in the store's transaction list. Outside story 4-3 scope; depends on backend API contract. [`src/stores/useTransactionStore.ts` — `importOFX`]
- **`sortWithMRU` recomputed every render without `useMemo`** — O(N) envelope sort per render per queue item; negligible with current envelope counts but worth memoizing when list grows. [`src/features/transactions/UnknownMerchantQueue.tsx:22`]
- **`_tempIdCounter` module-level singleton (story 4-3 context)** — Same pre-existing concern as logged above; no new risk introduced by 4-3 changes. [`src/stores/useTransactionStore.ts:31`]

## Deferred from: code review of 4-4-substring-rule-builder-creating-rules-inline (2026-04-08)

- **`isHighlighted` first-occurrence only** — if `selectedSubstring` appears multiple times in the payee, only the first occurrence is highlighted; visual ambiguity for repeated-char payees; the stored rule is functionally correct (substring match anywhere), but the highlight misrepresents which occurrence the user selected. [`src/features/merchant-rules/SubstringRuleBuilder.tsx:31`]
- **No cancel mechanism for drag released outside component** — releasing mouse outside the component always commits the last in-component `dragEnd` position; user cannot abort a drag in progress. Pre-existing design choice; add Escape key cancel or right-click cancel if UX feedback surfaces this. [`src/features/merchant-rules/SubstringRuleBuilder.tsx:39`]
- **`formatTxDate` uses local midnight, not UTC** — `new Date(date + 'T00:00:00')` parses as local time; in UTC-offset timezones the displayed date can be one calendar day off. Pre-existing pattern repeated from earlier queue code. [`src/features/transactions/UnknownMerchantQueue.tsx:18`]
- **Stale MRU ids never pruned when envelopes deleted** — `mruIds` state retains deleted envelope IDs permanently, silently consuming one of the three MRU slots. Filter against current `envelopes` prop in `sortWithMRU` when the envelope list can be mutated at runtime. [`src/features/transactions/UnknownMerchantQueue.tsx:27`]
- **Silent queue mismatch when queueId has no matching transaction** — `filter(Boolean)` drops unresolved IDs silently; header count and parent's `queueIds.length` expectation diverge with no diagnostic. Add a console.warn or assert when IDs are missing. [`src/features/transactions/UnknownMerchantQueue.tsx:57`]
